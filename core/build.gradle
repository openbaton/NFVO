repositories {
    mavenCentral()
}

dependencies {
    compile project(':vim:vim-interfaces')

    runtime project(':core:core-implementation')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}