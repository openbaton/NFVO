apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    runtime project(':repositories:repositories-implementation')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}