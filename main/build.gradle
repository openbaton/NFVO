dependencies {
    compile 'org.apache.activemq:activemq-pool:5.9.0'

    compile project(':repositories:repo-int')
    compile project(':catalogue')

    runtime project(':core')
    runtime project(':api')
    runtime project(':gui')
    runtime project(':cli')
    runtime project(':vim')
    runtime project(':repositories')
    runtime project(':vnfm')
}